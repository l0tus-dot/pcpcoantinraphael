cd C:\Users\Raphaël\PCP

# 1. Vérifier remote
git remote -v

# 2. Si besoin, corriger l’URL (remplace par tes infos)
git remote set-url origin git@github.com:TON_USER/TON_REPO.git

# 3. Tester SSH
ssh -T git@github.com

# 4. Si SSH OK, faire le push
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main

Pour PUSH :

cd C:\Users\Raphaël\PCP

# Vérifie que tu as bien des fichiers à push
git status

# Ajoute et commit
git add .
git commit -m "Initial commit"

# Renomme la branche en main (si ce n’est pas déjà fait)
git branch -M main

# Push vers GitHub
git push -u origin main